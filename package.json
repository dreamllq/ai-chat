{
  "name": "ai-chat-monorepo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm -C apps/demo dev",
    "build": "pnpm -C packages/ai-chat build",
    "test": "pnpm -C packages/ai-chat test",
    "type-check": "pnpm -r run type-check"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vite": "^5.0.0",
    "vue": "^3.4.0",
    "@vitejs/plugin-vue": "^5.0.0"
  }
}
